import classes from "./SushiMenuSection.module.css";

const SushiMenuSection = () => {
  return (
    <section className={classes.main}>
      <h1>Sushi Menu</h1>
    </section>
  );
};

export default SushiMenuSection;
